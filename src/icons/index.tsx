import { Eye, Lock, Palette, Sparkles, Shield, Zap } from 'lucide-react';


const iconMap: Record<string, React.ReactNode> = {
    lock: <Lock />,
    eye: <Eye />,
    palette: <Palette />,
    sparkles: <Sparkles />,
    shield: <Shield />,
    zap: <Zap />
};

export default iconMap