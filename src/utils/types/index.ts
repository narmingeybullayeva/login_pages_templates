export interface loginTemplate {
    id: number;
    title: string;
    description: string;
    preview: string;
    path: string;
    iconName: string;
    color: string;
    category: string;
    featured?: boolean;
}